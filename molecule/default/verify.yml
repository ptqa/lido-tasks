---
- name: Converge
  hosts: all
  gather_facts: true
  become: true
  tasks:
    - name: Include docker tests
      ansible.builtin.import_tasks:
        file: tests/docker.yml
    - name: Include http tests
      ansible.builtin.import_tasks:
        file: tests/http.yml
